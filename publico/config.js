/**
 * Onde está a API (o backend Node no Railway).
 *
 * Deixe VAZIO se o próprio backend serve esta tela — é o caso quando você abre
 * o endereço do Railway direto. Aí o front fala com a API na mesma origem e não
 * há nada para configurar.
 *
 * Preencha SÓ se você hospedar esta pasta separada (GitHub Pages, Netlify,
 * Cloudflare Pages). Ponha o endereço do backend, sem barra no fim:
 *
 *   window.API = 'https://espremedor-laranja-production.up.railway.app';
 *
 * Nesse caso o backend também precisa liberar este endereço na variável
 * ORIGENS_PERMITIDAS — veja o README.
 */
window.API = 'https://espremedorlaranja-production.up.railway.app';
